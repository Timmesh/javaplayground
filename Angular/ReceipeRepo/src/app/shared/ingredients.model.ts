export class Ingredient {

  /**
   * This is similar to adding name and amount as attribte.
   * @param name , not null
   * @param amount  not null
   */
  constructor(public name: string, public amount: number) {}
}
